import React from 'react';

const EmargementList: React.FC = () => {
    return (
        <div>
            <h1>Liste des Émargements</h1>
            {/* Logique pour afficher la liste des émargements */}
        </div>
    );
};

export default EmargementList;